# 数据库相关配置
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/lab_database?characterEncoding=utf8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
# 表字段有下划线自动折叠

mybatis:
  configuration:
    mapUnderscoreToCamelCase: true